spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/library
    username: library
    password: library
  jpa:
    properties:
      hibernate:
        temp:
          use_jdbc_metadata_defaults: false